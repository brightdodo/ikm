package com.test;

import java.util.Locale;

public class LocaleTest {
    public static void main(String[] args) {
        Locale locale = new Locale("");
        Locale locale1 = new Locale("","");
        //Locale locale2 = new Locale(Locale.ENGLISH);
    }
}
